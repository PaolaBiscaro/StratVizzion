### Para rodar o docker compose:

```
docker compose up -d
```

é necessario ter o **docker e wsl** instalado no computador.
Docker(podendo ser via cli ou desktop) é necessario estar rodando para subir o container.
